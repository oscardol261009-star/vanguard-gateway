--!strict
-- Module partagé (accessible côté serveur ET client via ReplicatedStorage.Shared).
-- On y mettra les réglages du jeu quand on le construira.

local Config = {
	NomDuJeu = "Mon Jeu",
	Version = "0.1.0",
}

return Config
